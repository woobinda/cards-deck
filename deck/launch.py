# -*- coding: utf-8 -*-
from models import Card, Deck
from views import *

if __name__ == '__main__':
    # The script to start the application from the console
    print('\n\nТекущая колода %s карты: \n\n' % (len(deck)))
    print(deck)
    print(menu)
    run()
